# TASK-NNNN: Название задачи

## Статус

Предложена | Готова к реализации | В реализации | На ревью | Принята | Требуются изменения | Заблокирована | Заменена

## Исходный запрос

Кратко зафиксируй пользовательский запрос.

## Нормализация запроса

- Удалено как шум: пункт или `нет`.
- Сохраненные ограничения: пункт или `нет`.
- Принятые допущения: пункт или `нет`.

## Уточненная задача

Опиши задачу так, чтобы ее можно было передать агентам.

## Цель

Опиши проверяемый результат.

## Контекст проекта

- Связанные документы: ссылки или `нет`.
- Затронутые страницы или маршруты: список или `нет`.
- Ограничения задачи: список или `нет`.
- Дополнительные материалы: ссылки или `нет`.

## Границы

Входит:

- Пункт.

Не входит:

- Пункт.

Запрещено:

- Пункт.

## Связанные артефакты

- ADR: ссылка или `нет`.
- Спецификация: ссылка или `нет`.
- Все ревью: ссылки или `нет`.
- Последнее ревью: ссылка или `нет`.
- Предыдущая задача: ссылка или `нет`.

## Критерии готовности задачи

Задача готова к реализации или проектному уточнению, когда:

1. Цель сформулирована.
2. Границы указаны.
3. Нужные входные документы перечислены.
4. Открытые вопросы не блокируют следующий шаг.

## Открытые вопросы

- Вопрос или `нет`.

## Следующий агент

Укажи один вариант:

- `agents/architect-spec-agent.md`;
- `agents/implementation-agent.md`;
- `agents/review-agent.md`;
- `agents/task-closeout-agent.md`;
- пользователь.

## Итог закрытия

- Принятое ревью: ссылка или `нет`.
- Закрыто: дата или `нет`.
- Обновленные документы: список или `не требуется`.
- Следующие задачи: ссылки, кандидаты или `нет`.
- Кандидаты на улучшение процесса: список или `нет`.

## История статусов

- Статус: дата или краткое описание изменения.

## Правила обновления

Обновляй этот файл после каждого этапа:

1. После создания ADR, если он нужен.
2. После создания спецификации, если она нужна.
3. После передачи в реализацию.
4. После завершения реализации.
5. После каждого ревью.
6. После возврата на доработку.
7. После закрытия.

Одновременно обновляй `docs/tasks/index.md`.
